# Local development defaults to http://localhost:5000 when this variable is omitted.
# Vercel production should point this at the public Render gateway URL/custom domain.
VITE_API_URL=http://localhost:5000
